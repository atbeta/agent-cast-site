{
  "framework": null,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm rebuild esbuild sharp && pnpm build",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist"
}